pipeline {
  agent any
  stages {
		stage('maven打包') {
		    agent {
				docker {
					image 'maven:3.5.4-jdk-8'
					args '-v /root/.m2:/root/.m2'
				}
		    }
		  
		    steps {
				sh 'mvn -B -DskipTests clean package'
		    }
		}
		
		stage('备份旧war包') {
			steps {
				script{
					sh 'pwd'
					def backup = '/home/webapps_backup/CounterWebApp_'+currentBuild.number+'.war'
					def dest = '/home/webapps/CounterWebApp.war'
					def src = '/var/jenkins_home/workspace/测试远程部署/target/CounterWebApp.war'
					sh "cp  ${src} ${dest}"
					sh "cp  ${dest} ${backup}"
					echo 'backup success'
				}
			}
		}
		
		stage('远程发布到服务器') {
			agent{
				docker{
					image 'adhax/alpine-sshpass'
					args '-v /home/jenkins_deploy:/home/jenkins_deploy'
				}
			}
			
			steps {
				script{
					sh 'pwd'
					sh 'cd /home/jenkins_deploy'
					sh 'ls -all'
					sh "sh auto_deploy.sh"	
					echo "auto_deploy success"
				}
			}
		}
	}
}